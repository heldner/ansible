---
- hosts: server
  gather_facts: true
  become: false
  collections:
    - heldner.things
  roles:
    - apt
    - console-setup
    - hardending
    - modprobe
    - shadowsocks
    - ssh
    - tmux
    - unbound
    - wireguard
    - zsh
